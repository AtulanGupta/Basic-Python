#ternary operator works if a certain condition is met and do another if not.
# is this case the condition must be written in a line.
#result A if condition else B
print('yes!'if False else 'no!!')
a,b=30,40
min=(a if a<b else b)
print(min)
